<form method="post" action="{{ path('app_reservation_form_crud_delete', {'id': reservation_form.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_form.id) }}">
    <button class="btnR">Delete</button>
</form>
